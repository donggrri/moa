// Public browser configuration for the GitHub Pages build.
// Replace these placeholders with the Supabase project URL and publishable key.
// The publishable key is safe to expose only when the database RLS policies are correct.
window.MOA_SUPABASE_CONFIG = {
  url: '',
  publishableKey: ''
};
