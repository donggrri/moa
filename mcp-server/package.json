{
  "name": "moa-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local STDIO MCP server for the Moa shared-space data tools.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test server.test.mjs"
  }
}
