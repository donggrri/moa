# 모아 프로젝트 진행 계획과 검토

최초 작성: 2026-08-01  
상태 갱신: 2026-08-17

## 현재 진행 상태 (2026-08-17)

완료된 기능의 원본은 [`docs/FEATURES.md`](docs/FEATURES.md)다. 다음 세션 손 위치는 [`docs/CONTINUE.md`](docs/CONTINUE.md)다.

할일의 기준 저장소는 브라우저 localStorage가 아니라 **Supabase**다. 웹앱과 MCP는 같은 스키마·RPC를 쓴다.

배포 주소: [https://donggrri.github.io/moa/](https://donggrri.github.io/moa/)  
로컬: `http://localhost:5173/`

| Phase | 내용 | 상태 |
|---|---|---|
| 0. 제품 가설 검증 | 3~5쌍 실사용 | 미착수 |
| 1. MVP 정리 | 화면·아이디어·연기 | 대부분 완료. 반복 수정 정책·오늘 화면의 지연 할일 표시는 미결 |
| 2. 서버·인증·공간 | PostgreSQL, 이메일 로그인, 초대, RLS | 코드·실연결 완료. 두 계정 초대 검증은 미완 |
| 3. 할일·공동 편집·반복 | CRUD, 완료, 다음 회차 RPC, Realtime | 코드 완료. 동시 완료 실기기 검증은 미완 |
| 4. 알림·카카오 | Reminder, 웹 푸시, 카카오 공유 | 미착수. 화면의 “다음 알림”은 마감일 표시일 뿐 |
| 5. 베타·운영 | 약관, 계정 삭제, 지표 | 미착수 |

추가로 반영된 것:

- GitHub Pages에 publishable key 연결. 회사 PC는 Pages URL로 로그인하면 된다.
- 로컬 MCP HTTP 서버(`mcp-server/`). 이 PC가 켜져 있을 때만 처리하고, Cursor는 `127.0.0.1:8787` 클라이언트다. 이후 서버는 라즈베리파이로 옮긴다. 실제 조회는 `mcp-server/.env`가 필요하다.
- 날짜가 지난 할일은 **삭제되지 않는다**. 오늘 화면에서는 빠지고, 전체 할일에서 지연으로 보인다.

## 결론

화면과 사용 흐름은 유지한다. 서버를 할일의 기준 저장소로 두는 방향은 이미 적용됐다. 카카오톡은 계속 선택 가능한 전송 채널로 두고, 지금은 공동 사용 검증과 알림을 먼저 한다.

권장 순서는 다음과 같다.

    사용자 흐름 검증
      → 도메인·데이터 경계 고정
      → 서버·인증·초대
      → 공동 편집·반복 회차
      → 앱 푸시
      → 카카오톡 공유·메시지
      → 베타 운영

## 제품 범위

### 이번 제품이 해결할 문제

- 두 사람이 하나의 공동 공간을 만든다.
- 할일을 만들고 담당자를 정한다.
- 완료·연기·재배정을 간단히 처리한다.
- 반복되는 생활 약속을 자동으로 다음 회차로 이어간다.
- 아직 구체화하지 않은 공동생활 아이디어를 저장하고 필요할 때 할일로 전환한다.
- 필요한 알림만 앱과 메신저로 전달한다.

### 초기에는 하지 않을 것

- 일반 카카오톡 개인 대화방의 내용을 읽거나 분석하기
- 카카오톡을 완전한 챗봇 입력창으로 사용하는 기능
- 모든 메신저를 동시에 지원하기
- 처음부터 iOS·Android 네이티브 앱을 별도로 개발하기
- 포인트·벌금·결제 같은 경쟁적 기능
- 개인 전용 아이디어 보관함과 아이디어 댓글·이미지 기능

처음부터 카카오톡 대화 읽기를 제품의 전제로 삼지 않는다. 카카오톡 메시지 API는 같은 서비스 내 사용자 간 발송을 지원하지만, 친구에게 발송하려면 추가 권한·동의가 필요하고 발송량 제한도 있다. 카카오톡 채널 API도 채널 관계와 고객 관리 중심이므로, 핵심 할일 데이터는 자체 서비스에 두어야 한다. [카카오톡 메시지 API](https://developers.kakao.com/docs/ko/kakaotalk-message/rest-api) · [카카오톡 채널 API](https://developers.kakao.com/docs/ko/kakaotalk-channel/common)

## 현재 MVP에 대한 평가

화면은 다음을 검증하기에 적합하다.

- 공동 공간을 선택하는 흐름
- 오늘 할일, 전체 할일, 반복 일정, 멤버 화면
- 담당자 지정과 완료 처리
- 반복 할일의 다음 회차 생성 경험
- 공동 아이디어 저장과 할일 전환 흐름
- 초대 코드·링크 UI

2026-08-15 기준으로 서버 쪽은 이미 바뀌었다.

- 업무 데이터는 Supabase에 저장한다. 웹과 MCP가 같은 RPC를 사용한다.
- 초대는 `join_space`로 검증한다.
- 반복 다음 회차는 서버 RPC와 `(recurrence_rule_id, due_date)` 유일성으로 처리한다.
- User와 Membership은 테이블로 분리되어 있다.

아직 프로토타입인 것:

- 두 브라우저·두 계정으로 초대·동시 완료를 아직 확인하지 않았다.
- 알림 예약·재시도·전송 결과 테이블이 없다.
- 오늘 화면은 당일 마감만 보여 지연 할일이 빠진 것처럼 보인다.
- “이번 회차만 / 앞으로 모든 회차” 수정 정책이 없다.
- 소유권 이전 UI가 없다.
- 카카오 로그인 및 실제 카카오톡 전송은 연결되지 않았다.

프론트엔드는 버리고 다시 짜기보다, **현재 화면을 유지하고 서버 계약을 기준으로 기능을 붙이는 것**이 맞다.

## 권장 기술 방향

### 1차 제품: 웹앱/PWA

현재 결과물이 웹 기반이므로 먼저 반응형 웹앱으로 출시한다.

- 프론트엔드: TypeScript 기반 React 또는 Next.js
- 데이터베이스: PostgreSQL
- 인증: 카카오 OAuth와 서비스 자체 세션
- 서버: API route 또는 별도 Node.js API
- 예약 작업: 서버 작업 큐 또는 관리형 스케줄러
- 실시간 갱신: WebSocket 또는 DB 변경 구독
- 푸시: 웹 푸시를 우선 검토하고, 네이티브 앱 단계에서 FCM/APNs 추가

구현 속도를 우선하면 현재 정적 화면을 한 번에 전부 재작성하지 말고, 다음 순서로 바꾼다.

1. 현재 화면의 데이터 접근 함수를 API 호출로 교체
2. localStorage 저장을 서버 저장으로 교체
3. 로그인·초대·권한 검사를 추가
4. 반복 회차와 알림을 서버 작업으로 이동
5. 이후 컴포넌트와 타입을 정리

### 카카오 연동 순서

1. 카카오톡 공유: 공동 공간 초대와 할일 링크 공유
2. 카카오 로그인: 서비스 사용자 식별
3. 앱 푸시: 기본 알림 채널
4. 카카오톡 메시지 API: 권한과 쿼터를 확인한 제한적 실험
5. 카카오톡 채널·비즈메시지: 사용자가 충분히 확보된 후 검토

카카오 로그인은 인가 코드와 토큰 발급을 서비스 서버에서 처리하는 구조로 설계해야 한다. state 검증, 리다이렉트 URI, 토큰 보관 위치를 초기부터 정한다. [카카오 로그인 REST API](https://developers.kakao.com/docs/ko/kakaologin/rest-api)

## 서버 도메인 초안

| 개념 | 책임 |
|---|---|
| User | 서비스 사용자와 로그인 식별자 |
| Space | 공동생활 단위 |
| Membership | 사용자와 공간의 참여 관계 |
| Invite | 제한된 기간과 사용 조건을 가진 참여 자격 |
| Task | 한 날짜에 실행되는 구체적인 할일 |
| Idea | 아직 담당자와 마감 시각을 정하지 않은 공동생활의 생각 |
| RecurrenceRule | 다음 할일 회차의 생성 규칙 |
| Reminder | 언제 어떤 사용자에게 알릴지에 대한 약속 |
| IntegrationConnection | 외부 채널 연결 및 동의 상태 |
| DeliveryLog | 알림 발송 시도와 결과 |

핵심 관계는 다음과 같다.

    User ──< Membership >── Space
    Space ──< Task
    Space ──< Idea
    Idea ──> Task (전환 시 선택적으로 연결)
    Task ──> RecurrenceRule (선택)
    Task ──< Reminder
    User ──< IntegrationConnection
    Reminder ──< DeliveryLog

반복 일정은 RecurrenceRule과 실제 실행되는 Task를 구분한다. “매주 토요일 침구 세탁”이라는 규칙과 “2026-08-08 회차”는 같은 개념이 아니다.

## 단계별 실행 계획

아래 기간은 1인 개발 기준의 대략적인 순서이며, 카카오 권한 심사 기간은 별도로 본다.

### Phase 0. 제품 가설 검증 — 3~5일

할 일:

- 첫 고객을 신혼부부·커플로 좁힌다.
- 3~5쌍에게 현재 MVP를 직접 사용하게 한다.
- 실제로 반복되는 생활 약속 20개를 수집한다.
- 사용자가 가장 싫어하는 순간을 확인한다: 등록, 알림, 담당자 변경, 완료 확인 중 무엇인지.

완료 기준:

- 첫 공동 공간 생성부터 첫 할일 완료까지의 흐름이 설명 없이 진행된다.
- 사용자가 3일 이상 다시 방문한다.
- “카카오톡으로 무엇을 하고 싶은가”를 공유, 알림, 입력 중 하나로 좁힌다.

### Phase 1. 현재 MVP 정리 — 3~5일

할 일:

- CONTEXT.md의 용어를 기준으로 화면 문구 정리
- 날짜·시간·타임존 정책 결정
- 반복 규칙과 할일 회차의 UI 동작 명확화
- 아이디어와 할일의 경계 및 아이디어 전환 동작 확정
- 초대 코드 UI와 실제 초대의 차이를 표시
- 테스트 초기화 기능 또는 데모 데이터 초기화 추가

완료 기준:

- “반복 일정을 수정했을 때 이미 생성된 회차가 어떻게 되는가”에 대한 답이 정해진다.
- 완료 취소, 연기, 담당자 변경의 상태 전이가 정해진다.
- 아이디어를 전환해도 원본 아이디어가 보존되고 생성된 할일과 연결된다.

### Phase 2. 서버·인증·공간 — 1~2주

할 일:

- PostgreSQL 스키마 생성
- 카카오 로그인과 내부 User 매핑
- Space·Membership 분리
- 초대 생성·사용·만료·취소 API
- 공간별 권한 검사
- Row-Level Security 또는 동일 수준의 서버 권한 검사

완료 기준:

- 두 브라우저에서 서로 다른 사용자로 로그인할 수 있다.
- 한 사용자가 만든 공간에 다른 사용자가 초대 코드로 참여한다.
- 다른 공간의 할일을 조회하거나 수정할 수 없다.

### Phase 3. 할일·공동 편집·반복 — 1~2주

할 일:

- 할일 CRUD
- 담당자 지정과 재배정
- 완료·완료 취소·연기
- 반복 규칙 생성
- 서버에서 다음 회차 생성
- 실시간 또는 짧은 주기의 데이터 갱신
- 중복 생성 방지와 감사 가능한 상태 변경 기록

완료 기준:

- 두 사용자가 같은 할일의 변경을 확인한다.
- 같은 회차를 동시에 완료해도 다음 회차가 한 번만 생성된다.
- 과거 회차의 완료 기록이 미래 회차 수정으로 사라지지 않는다.

### Phase 4. 알림·카카오 연결 — 1~2주

할 일:

- 사용자별 알림 켜기·끄기
- 조용한 시간
- 마감 전 알림과 미완료 요약 알림
- 앱 푸시를 기본 전송 채널로 구현
- 카카오톡 공유로 초대·할일 링크 연결
- 카카오톡 메시지 API는 본인 알림부터 제한적으로 검증
- 카카오톡 친구 발송은 권한·동의·쿼터 확인 후 별도 기능 플래그로 운영

완료 기준:

- 알림이 중복 발송되지 않는다.
- 발송 실패를 재시도하고 결과를 확인할 수 있다.
- 사용자가 언제든 카카오톡 알림을 끌 수 있다.

### Phase 5. 소규모 베타·운영 — 1~2주

할 일:

- 개인정보 처리방침·서비스 이용약관 준비
- 계정 연결 해제와 데이터 삭제
- 오류 로그·알림 발송 로그·기본 지표 추가
- 초대 남용과 API 호출량 제한
- 백업·복구 시나리오 점검
- 10~20개 공동 공간 대상 베타 운영

완료 기준:

- 초대, 로그인, 할일 완료, 알림 실패를 운영자가 추적할 수 있다.
- 장애가 났을 때 사용자에게 상태를 설명할 수 있다.
- 7일차에도 실제로 사용하는 공간이 확인된다.

### Phase 6. 확장 기능

베타에서 사용성이 확인된 뒤에만 진행한다.

- 자연어로 할일 추가
- 장보기 목록과 할일 연결
- 집안일 부담량 시각화
- Telegram·Slack·Discord 등 추가 채널
- React Native/Expo 기반 모바일 앱
- 알림톡·친구톡 같은 사업용 메시지 검토

## 계획 검토: 반드시 보완해야 할 부분

### 위험도 높음

**반복 회차 중복 생성**

브라우저에서 다음 회차를 만들면 두 사용자가 동시에 완료할 때 중복이 생길 수 있다. 서버 RPC와 `recurrence_rule_id + due_date` 유일성 제약은 들어갔다. 두 브라우저 동시 완료로 한 번만 생기는지는 아직 확인하지 않았다.

**로컬 상태와 실제 공동 사용의 차이**

서버 저장은 연결됐다. 화면이 편하다는 것과 두 사람이 계속 쓴다는 것은 다르다. Phase 0에서 실제 두 기기 사용을 반드시 확인한다.

**카카오톡을 핵심 입력 경로로 삼는 것**

카카오 API 권한과 쿼터에 제품 전체가 종속되면 출시가 지연된다. 앱을 기준 저장소로 두고, 카카오톡은 초대와 알림부터 시작한다.

### 위험도 중간

**사용자와 공간 참여자의 혼동**

사용자는 서비스 전체의 사람이고, 참여자는 특정 공간 안의 관계다. Membership을 별도 개념으로 두지 않으면 탈퇴, 재초대, 권한 변경에서 문제가 생긴다.

**반복 규칙 수정 정책**

“이번 회차만 수정”, “앞으로 모든 회차 수정”, “이미 완료한 과거 회차 보존”을 구분해야 한다. 이 정책을 Phase 1에서 결정하지 않으면 데이터 모델을 다시 바꾸게 된다.

**알림 피로**

할일마다 메시지를 보내면 서비스가 잔소리 도구가 된다. 조용한 시간, 요약 알림, 사용자별 채널 선택을 MVP 후반에 반드시 넣는다.

**날짜와 타임존**

서버 시간대, 사용자의 시간대, 공간의 기준 시간대를 무엇으로 할지 결정해야 한다. 초기 타깃이 한국 사용자라면 공간 기본 시간대를 Asia/Seoul로 두되, 데이터는 명확한 시간대 정보를 갖게 한다.

## 측정할 지표

- 활성화: 공동 공간 생성 후 10분 안에 첫 할일을 만든 비율
- 초대 성공률: 초대 발급 후 상대방이 참여한 비율
- 공동 사용률: 공간 생성자 외 다른 참여자가 첫 주에 할일을 완료한 비율
- 7일 유지율: 7일 후에도 할일을 추가하거나 완료한 공간 비율
- 알림 유효율: 알림 후 일정 시간 안에 완료된 할일 비율
- 알림 해제율: 알림을 끄거나 앱을 삭제한 비율

## 다음 작업 순서

상세 손 위치는 [`docs/CONTINUE.md`](docs/CONTINUE.md)를 따른다.

1. `mcp-server/.env`를 채운 뒤 `npm run http:install`로 이 PC 로그온 시 HTTP 서버를 띄우고, Cursor HTTP 클라이언트로 할일 조회·추가를 확인한다. 체크리스트는 `tests/release/mcp-http.md`. 라즈베리파이 이전은 그다음이다.
2. 두 브라우저에서 다른 계정으로 로그인·초대 참여·할일 완료·반복 동시 완료를 확인한다.
3. 오늘 화면에 날짜가 지난 미완료 할일을 지연으로 보여 줄지 결정한다.
4. 반복 규칙 수정 정책(“이번 회차만 / 앞으로 모든 회차 / 과거 완료 보존”)을 확정한다.
5. 소유권 이전 UI를 넣을지 결정한다. DB는 마지막 owner 삭제를 이미 막는다.
6. 그다음에 Reminder·웹 푸시를 만들고, 카카오톡은 초대 공유부터 연결한다.

## 최종 판단

계획의 큰 방향은 타당하다. 카카오톡을 초기에 제품 중심으로 두지 않은 것도 맞다. 서버·권한·반복 회차 코드는 들어갔으므로, 다음은 기능을 더 늘리기보다 **두 사람 실사용 검증**과 **오늘/지연 할일 UX**, 그다음 **알림**에 쓰는 것이 좋다.
